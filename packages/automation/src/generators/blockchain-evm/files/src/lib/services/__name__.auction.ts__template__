import { evmBasedAuction, EvmBasedWeb3 } from '@tatumio/shared-blockchain-evm-based'
import { EvmBasedBlockchain } from '@tatumio/shared-core'
import { <%= firstLetterUpperCaseName %>Service } from '@tatumio/api-client'

export const <%= name %>AuctionService = (args: { blockchain: EvmBasedBlockchain; web3: EvmBasedWeb3 }) => {
  return {
    ...evmBasedAuction({ ...args, broadcastFunction: <%= firstLetterUpperCaseName %>Service.<%= name %>Broadcast }),
  }
}
