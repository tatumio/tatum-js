import { EthTx as EthLikeTx } from '@tatumio/tatum-core'

/**
 *
 * @export
 * @interface {{ pascalCase slug}}Tx
 */
export interface {{ pascalCase slug}}Tx extends EthLikeTx {
  effectiveGasPrice?: string
  hash?: string
  logsBloom?: string
  type?: string
}
